{
  "name": "digigamez",
  "version": "1.0.0",
  "description": "Nederlandse spellenbundel voor kinderen (2-7 jaar) op een 75-inch digibord",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build && npm run check:compat",
    "check:compat": "node scripts/check-compat.mjs",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
